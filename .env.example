# PIA Industrial - Environment Configuration

# ---------------------------------------------------------
# Frontend Configuration
# ---------------------------------------------------------
# Optional: URL of the backend API. Defaults to http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000

# ---------------------------------------------------------
# Backend / AI Configuration
# ---------------------------------------------------------
# Note: For the hackathon M77 release, PIA Industrial is fully offline 
# and deterministic. The Knowledge Graph, Copilot, and Intelligence engines 
# run deterministically without requiring external API keys.
# No OPENAI_API_KEY or GEMINI_API_KEY is required for the demo.

# GITHUB_TOKEN=  # Used ONLY by legacy PIA-GH scripts (archived)
